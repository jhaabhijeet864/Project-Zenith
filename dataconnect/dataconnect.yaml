specVersion: "v1"
serviceId: "zenith"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "zenith-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
